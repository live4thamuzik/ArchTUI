{
  "script": "scripts/tools/run_as_user.sh",
  "description": "Execute a command as a non-root user inside arch-chroot. Used for operations that require non-root privileges like makepkg.",
  "destructive": false,
  "version": "1.0",
  "needs_stdin": false,
  "valid_exit_codes": [0, 1],
  "required_env": [],
  "optional_env": []
}
