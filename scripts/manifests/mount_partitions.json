{
  "script": "scripts/tools/mount_partitions.sh",
  "description": "Comprehensive mount/unmount partition management. Mount devices to directories, unmount safely with recursive support, list mounts, and show partition info.",
  "destructive": false,
  "version": "1.0",
  "needs_stdin": false,
  "valid_exit_codes": [0],
  "required_env": [],
  "optional_env": []
}
